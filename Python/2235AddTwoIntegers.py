# 1. Just add them together and return the sum.
class Solution:
    def sum(self, num1: int, num2: int) -> int:
        return num1 + (num2)
        

# 2. Add method to add two integers.
class Solution:
    def sum(self, num1: int, num2: int) -> int:
        return num1.__add__(num2)
        